# Deployment pins (Streamlit Community Cloud, Python 3.12). Exact versions the
# bundles were built/pickled with -- keep in sync with the training env.
streamlit==1.59.2
scikit-learn==1.9.0
pandas==3.0.3
numpy==2.5.1
joblib==1.5.3
requests==2.34.2
matplotlib==3.11.1
plotly==6.9.0
